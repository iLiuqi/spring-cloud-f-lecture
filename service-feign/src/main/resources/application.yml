spring:
  application:
    name: service-feign

server:
  port: 8765

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

feign:
  hystrix:
    enabled: true   # Feign自带断路器，默认关闭